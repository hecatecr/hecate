# Task ID: 11
# Title: Setup Lex Shard Foundation
# Status: pending
# Dependencies: 2
# Priority: high
# Description: Initialize the hecate-lex shard with dependency on hecate-core
# Details:
In shards/hecate-lex/:
- Create shard.yml:
```yaml
name: hecate-lex
version: 0.1.0
crystal: ">=1.17.0"

dependencies:
  hecate-core:
    path: ../hecate-core

license: MIT
```
- Set up src/hecate-lex.cr requiring hecate-core
- Create src/hecate/lex.cr module structure
- Initialize spec/spec_helper.cr
- Add README.md with lexer examples

# Test Strategy:
Verify shards install pulls in hecate-core, ensure compilation with core dependency, run empty spec suite

# Subtasks:
## 1. Create Lex Shard Directory Structure [pending]
### Dependencies: None
### Description: Set up the directory structure for hecate-lex shard with all necessary folders
### Details:
Create shards/hecate-lex directory with src/, spec/, and src/hecate/ subdirectories. Ensure proper file permissions and directory hierarchy.

## 2. Configure shard.yml with Core Dependency [pending]
### Dependencies: 11.1
### Description: Create and configure shard.yml file with hecate-core dependency and project metadata
### Details:
Create shards/hecate-lex/shard.yml with name: hecate-lex, version: 0.1.0, crystal: >=1.17.0, dependencies pointing to ../hecate-core via path, and MIT license. Include author information and description.

## 3. Implement Main Entry Point [pending]
### Dependencies: 11.1, 11.2
### Description: Create src/hecate-lex.cr as the main entry point that requires hecate-core
### Details:
Create src/hecate-lex.cr with require "./hecate/lex" and proper module initialization. Ensure it properly requires hecate-core and exposes the main Hecate::Lex module.

## 4. Setup Module Structure and Spec Helper [pending]
### Dependencies: 11.3
### Description: Create the Hecate::Lex module structure and initialize spec helper for testing
### Details:
Create src/hecate/lex.cr with module Hecate::Lex definition and VERSION = "0.1.0" constant. Create spec/spec_helper.cr requiring "spec", "../src/hecate-lex", and any necessary test utilities.

## 5. Create README with Lexer Examples [pending]
### Dependencies: 11.4
### Description: Write comprehensive README.md with lexer usage examples and documentation
### Details:
Create shards/hecate-lex/README.md with overview of the lexer shard, installation instructions, basic usage examples showing token definition DSL, integration with hecate-core diagnostics, and API documentation references.

